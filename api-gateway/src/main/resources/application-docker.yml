server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:password@discovery-server:8761/eureka

management:
  otlp:
    tracing:
      endpoint: http://tempo:4318/v1/traces