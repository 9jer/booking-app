server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres-property:5432/property_service_db
    username: postgres
    password: postgres
  kafka:
    bootstrap-servers: kafka:9092

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:password@discovery-server:8761/eureka
